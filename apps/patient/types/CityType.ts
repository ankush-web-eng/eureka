export interface City {
    name: string;
}

export interface Country {
    name: string;
    isoCode: string;
}

export interface State {
    name: string;
    isoCode: string;
    countryCode : string;
}